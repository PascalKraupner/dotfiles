---
- name: Create Symlink for snap classic
  file:
    src: /var/lib/snapd/snap
    dest: /snap
    state: link
  become: yes

- name: Install emoji font
  kewlfft.aur.aur:
    use: yay
    name:
      - ttf-twemoji

